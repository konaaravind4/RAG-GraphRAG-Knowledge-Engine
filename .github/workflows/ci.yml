name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install deps
        run: pip install faiss-cpu numpy langchain langchain-community sentence-transformers fastapi httpx pytest pytest-asyncio pydantic python-dotenv
      - name: Run tests
        run: python -m pytest tests/ -v --tb=short
